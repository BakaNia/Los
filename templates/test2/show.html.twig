{% extends 'base.html.twig' %}

{% block title %}Test2{% endblock %}

{% block body %}
    <h1>Test2</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ test2.id }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ test2.city }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('test2_index') }}">back to list</a>

    <a href="{{ path('test2_edit', {'id': test2.id}) }}">edit</a>

    {{ include('test2/_delete_form.html.twig') }}
{% endblock %}
